Module Module1

    Sub Main()
        Dim w As String
        Dim x, y, z As Integer
        w = Console.ReadLine()
        x = Val(w)
        y = 1911
        z = x - y
        Console.WriteLine("西元" & x & "民國" & z)
        Console.Read()

    End Sub

End Module
